[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q
markers =
    slow: heavy engine re-runs (large grids / non-corner); opt-in via `-m slow`
    parity: cross-engine JS<->Py checks (require node on PATH)
