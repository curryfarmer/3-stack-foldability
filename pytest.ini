[pytest]
testpaths = smoketest
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q
markers =
    slow: expensive tests that re-run the engine (e.g. the physical-testing acceptance oracle); deselect with -m "not slow"
