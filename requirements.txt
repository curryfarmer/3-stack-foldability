# Python deps for the triangle/ + square/ engines and the smoketest/ suite.
# Install:  python -m venv .venv && .venv/Scripts/pip install -r requirements.txt
# (equivalently: pip install -e .[test], see pyproject.toml)

# --- test suite (required for `pytest`) ---
pytest==9.1.0

# --- engine rendering (square/render/, triangle/tri/render_*.py) ---
numpy==2.4.6
matplotlib==3.10.9
pillow==12.2.0
